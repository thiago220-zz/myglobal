<?php
$this->headTitle('Editar permisos de rol');

$this->mainMenu()->setActiveItemId('roles');

$this->pageBreadcrumbs()->setItems([
            'Página de inicio'=>$this->url('home'),
            'Administrar roles'=>$this->url('roles'),
            'Editar rol'=>$this->url('roles', ['action'=>'edit', 'id'=>$role->getId()])
            ]);

$form->get('submit')
        ->setAttributes(['class'=>'btn btn-danger'])
        ->setValue('Guardar');

$form->prepare();
?>

<h1>Editar permisos de <?= $this->escapeHtml($role->getName()) ?></h1>

<div class="row">
    <div class="col-md-8">
        <?= $this->form()->openTag($form); ?>
        <table class="table table-striped table-bordered">
            <tr>
                <th>Nombre</th>
                <th>Descripción</th>
            </tr>
            <?php foreach ($allPermissions as $permission):
                $element = $form->get('permissions')->get($permission->getName());
            ?>
            <tr>
                <td>
                    <div class="checkbox">
                        <label><input type="checkbox" name="<?= $this->escapeHtmlAttr($element->getName()) ?>" 
                                      value="<?= $this->escapeHtmlAttr($element->getCheckedValue()) ?>"
                                      <?php if ($element->isChecked()) echo 'checked'?>
                                      <?php if ($element->getAttribute('disabled')) echo 'disabled'?>>
                            <?= $this->escapeHtml($element->getLabel()) ?>
                        </label>
                        <?= $this->formElementErrors($element); ?>                  
                    </div>
                </td>
                <td>
                    <?= $this->escapeHtml($permission->getDescription()) ?>
                </td>
            </tr>
            <?php endforeach; ?>
        </table>
        <?= $this->formElement($form->get('csrf')); ?>
        
        <?= $this->formElement($form->get('submit')); ?>
        
        <?= $this->form()->closeTag(); ?>
    </div>
</div>

<div class="row">
    <div class="col-md-6">
        
        
        
        
        
    </div>    
</div>   

